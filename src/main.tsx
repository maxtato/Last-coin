import React from "react";
import ReactDOM from "react-dom/client";
import LastCoin from "./LastCoin";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <LastCoin />
  </React.StrictMode>
);
